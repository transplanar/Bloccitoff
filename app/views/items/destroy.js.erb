<% if @item.destroyed? %>
  $('#item-' +<%= @item.id %>).hide();
  // $(".flash").html("<%# escape_javascript( "Task destroyed") %>" );
  //Original
  $('.flash').prepend("<div class='alert alert-success'><button type='button' class='close' data-dismiss='alert'>&times;</button> Task destroyed </div>");
  //Test
  // $('.flash').prepend("<%# escape_javascript( "<div class='alert alert-success'><button type='button' class='close' data-dismiss='alert'>&times;</button> Task destroyed </div>") ) %>");

<% else %>
  // REVIEW make sure this properly shows alert via flash div of application.html.erb
  $('.flash').prepend("<div class='alert alert-danger'><button type='button' class='close' data-dismiss='alert'>&times;</button><%= flash[:error] %></div>");
<% end %>
